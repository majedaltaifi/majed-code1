const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // I might not have this locally

// Alternative: Use a browser script or just manually tell the user they can add them.
// Actually, I can use the fetch script approach in a temporary HTML file.
